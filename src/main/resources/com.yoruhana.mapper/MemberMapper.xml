<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.yoruhana.mapper.MemberMapper">

    <!--회원가입-->
    <insert id="insertJoin" parameterType="com.yoruhana.entity.MemberVO">
        insert into members (mb_name, mb_email, mb_pw, mb_tel, mb_stat,mb_join_date)
                    values(#{mb_name},CONCAT(#{mb_email1},'@',#{mb_email2}),#{mb_pw},CONCAT(#{mb_tel1},'-',#{mb_tel2},'-',#{mb_tel3}),0,NOW())
    </insert>

</mapper>